import { createSlice } from "@reduxjs/toolkit";

interface Rooms {

}

const initialState = {

}

export const roomsSlice = createSlice({
  name: 'rooms',
  initialState,
  reducers: {},
  extraReducers: builder => {

  }
})